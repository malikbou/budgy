<div class="container sm mb-5">
  <h3><strong><%= @event.name %></strong></h3>
  <h6><%= @event.description %></h6>
  <p><%= "#{@event.start_date.strftime('%b %d, %Y')} to #{@event.end_date.strftime('%b %d, %Y')}" %></p>

<div class="card mb-3">
  <div class="card-body">
    <h6 class="card-title">Auto & Transport</h5>
    <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
    <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
  </div>
</div>

  <div class="card d-flex justify-content-between">
    <div class="mr-auto p-2">
      <h5>💵 Spending</h5>
    </div>
    <div class="align-items-end p-2">
      <div class="col">
        <strong><%= "#{@event.currency} #{@event.expenses.sum(:amount)}" %></strong>
      </div>
      <div class="col">
        <small><%= "#{@event.currency} #{@event.budget}" %></small>
      </div>
    </div>
  </div>

  <ul class="list-group">
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <h5>💵 Spending</h5>
        <span class="badge bg-dark">
          <%= "#{@event.currency} #{@event.expenses.sum(:amount)} over #{@event.currency} #{@event.budget}"  %>
        </span>
      </li>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <div class="d-flex flex-column">
          <div class="p-2">
            <h5>⏰ Daily Allowance</h5>
          </div>
          <div class="p-2">
            <p><%="#{@days_left} days left"%></p>
          </div>
        </div>
        <span class="badge bg-dark">
          <%="#{@event.currency} #{@daily_budget} per day"%>
        </span>
  </ul>

  <br>
  <%= link_to "Back to My Events", my_events_path, class: "btn btn-primary btn-sm" %>
  <%= link_to "Edit Event", edit_event_path(@event), class: "btn btn-secondary btn-sm" %>
  <%= link_to "Delete", event_path(@event),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger btn-sm" %>
</div>

<div class="container sm mb-5">
  <div class="d-flex mb-3">
    <div class="me-auto p-2">
      <h3><strong>Budget</strong></h3>
    </div>
    <div class="p-2">
      <%= link_to "Edit Budget", edit_event_budget_path(@event, @event.budgets.first), class: "btn btn-secondary" %>
    </div>
  </div>
  <ul class="list-group">
  <% @event.budgets.each do |budget| %>
    <% budget.attributes.each do |category, amount| %>
      <% if @excluded_categories.exclude?(category) %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <%= label_tag category.capitalize %>
          <span class="badge bg-dark">
            <%= "#{@event.currency} #{amount}" %>
          </span>
        </li>
      <% end %>
    <% end %>
  <% end %>
</div>

<div class="container sm mb-5">
  <h3><strong>Budget vs Expenses</strong></h3>
  <div id="chart" class="container mb-3">
    <%= bar_chart [
    { name: "My Budget", data: @budget_totals },
    { name: "My Expenses", data: @clean_sort }
    ], overlap: true, library: {title: 'Side-by-Side Bar Chart', colors: ['#B2F', '#000']} %>
  </div>
</div>

<div class="container sm mb-5">
  <div class="d-flex mb-3">
    <div class="me-auto p-2">
      <h3><strong>Expenses</strong></h3>
    </div>
    <div class="p-2">
      <%= link_to "Add Expense", new_event_expense_path(@event), class: "btn btn-primary" %>
    </div>
  </div>
  <ul class="list-group">
    <% @event.expenses.each do |expense| %>
      <li class="list-group-item d-flex justify-content-between align-items-center mb-2">
        <strong><%= "#{Budget.human_attribute_name(expense.category)} - #{expense.description}" %></strong>
        <span class="badge bg-dark">
          <%= "#{@event.currency} #{expense.amount}" %>
        </span>
      </li>
    </ul>
      <%= link_to "Edit Expense", edit_event_expense_path(@event, expense), class: "btn btn-secondary btn-sm" %>
      <%= link_to "Delete", event_expense_path(@event, expense),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger btn-sm" %>
  <% end %>
</div>


<div class="container sm mb-5">
  <div class="d-flex">
    <div class="me-auto">
      <h5><strong>Transactions</strong></h5>
    </div>
    <div>
      <%= link_to "Add transaction", new_event_expense_path(@event), class: "link text-primary" %>
    </div>
  </div>
  <% @event.expenses.each do |expense| %>
    <div class="card mb-1">
      <div class="row d-flex justify-content-between align-items-center">
        <div class="col-2 p-3">
          <%= "#{Budget.human_attribute_name(expense.category)}" %>
        </div>
        <div class="col">
          <p class="pt-2">
            <%= expense.description %>
          </p>
          <p>
            Date Placeholder
          </p>
        </div>
        <div class="col">
          <p class="text-right">
            <span class="badge bg-dark">
              <%= "#{@event.currency} #{expense.amount}" %>
            </span>
          </p>
          <p>
            <%= link_to "🖋️", edit_event_expense_path(@event, expense), class: "btn btn-sm" %>
            <%= link_to "X", event_expense_path(@event, expense),
            data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger btn-sm" %>
          </p>
        </div>
      </div>
    </div>
  <% end %>
</div>


<div class="container sm mb-5">
  <h5>Transactions</h5>
  <div class="card">
    <div class="row d-flex justify-content-between align-items-center">
      <div class="col">
        One of three columns
      </div>
      <div class="col">
          <p>One of three columns</p>
          <p>One of three columns</p>
      </div>
      <div class="col">
        <p class="text-right">
          One of three columns
        </p>
      </div>
    </div>
  </div>
</div>


<div class="container sm mb-5">
  <div class="card">
    <div class="row d-flex justify-content-between align-items-center">
      <div class="col">
        <h5>💵 Spending</h5>
      </div>
      <div class="col ml-auto">
        <p>
          <strong><%= "#{@event.currency} #{@event.expenses.sum(:amount)}" %></strong>
        </p>
        <p>
          <small><%= "#{@event.currency} #{@event.budget}" %></small>
        </p>
      </div>
    </div>
  </div>
</div>

<div class="container sm mb-5">
  <div class="card">
    <div class="row d-flex justify-content-between align-items-center">
      <div class="col-8">
        <h5>💵 Spending</h5>
      </div>
      <div class="col ml-auto">
        <p class="text-right">
          <strong><%= "#{@event.currency} #{@event.expenses.sum(:amount)}" %></strong>
        </p>
        <p class="text-right">
          <small><%= "#{@event.currency} #{@event.budget}" %></small>
        </p>
      </div>
    </div>
  </div>
</div>

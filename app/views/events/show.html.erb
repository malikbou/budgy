<div class="container sm mb-5">
  <h2><strong><%= @event.name %></strong></h2>
  <h4><%= @event.description %></h4>
  <p><%= "From #{@event.start_date.strftime('%d/%m/%Y')} to #{@event.end_date.strftime('%d/%m/%Y')}" %></p>

  <h5><%= "Spending $XXX over #{@event.currency} #{@event.budget}" %></h5>
  <h5><%= "Daily Allowance $XXX over #{@event.currency} #{@event.budget}" %></h5>
  <% if @days_left.nil? %>
    <%= "Your trip hasn't started yet" %>
  <% else %>
    <%= "You have #{@days_left} days left" %>
  <% end %>
  <br>
  <%= link_to "Back to My Events", my_events_path, class: "btn btn-primary" %>
  <%= link_to "Edit Event", edit_event_path(@event), class: "btn btn-secondary" %>
  <%= link_to "Delete", event_path(@event),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
</div>

<div class="container sm mb-5">
  <h3><strong>Budget</strong></h3>
  <% if @event.budgets.empty? %>
    <%= link_to "Create Budget", new_event_budget_path(@event), class: "btn btn-primary" %>
  <% else %>
    <ul class="list-group">
      <% @event.budgets.each do |budget| %>
        <li class="list-group-item"> 🛩️  Flights: <%= budget.flights %></li>
        <li class="list-group-item"> Transport: <%= budget.transport %></li>
        <li class="list-group-item"> Accommodation: <%= budget.accommodation %></li>
        <li class="list-group-item"> Eating Out: <%= budget.eating_out %></li>
        <li class="list-group-item"> Entertainment: <%= budget.entertainment %></li>
        <li class="list-group-item"> Activities: <%= budget.activities %></li>
        <li class="list-group-item"> Shopping: <%= budget.shopping %></li>
        <li class="list-group-item"> Emergency: <%= budget.emergency %></li>
    </ul>
    <br>
      <%= link_to "Edit Budget", edit_event_budget_path(@event, budget), class: "btn btn-secondary" %>
      <%= link_to "Delete", event_budget_path(@event, budget),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
    <% end %>
  <% end %>
</div>

<div class="container sm mb-5">
  <h3><strong>Expenses</strong></h3>
  <%= "Total: #{@event.expenses.sum(:amount)}" %>
  <%= link_to "Add Expense", new_event_expense_path(@event), class: "btn btn-primary" %>
      <ul class="list-group">
        <% @event.expenses.each do |expense| %>
          <li class="list-group-item"> Description: <%= expense.description %></li>
          <li class="list-group-item"> Participants: <%= expense.participants %></li>
          <li class="list-group-item"> Amount: <%= expense.amount %></li>
          <li class="list-group-item"> Category: <%= expense.category %></li>
      </ul>
        <%= link_to "Edit Expense", edit_event_expense_path(@event, expense), class: "btn btn-secondary" %>
        <%= link_to "Delete", event_expense_path(@event, expense),
        data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
    <% end %>
</div>
